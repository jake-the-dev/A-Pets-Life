import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    // flexGrow: 1,
  },
  heading: {
    color: "black",
  },
  image: {
    marginInline: "15px",
    alignItems: "center",
  },
}));
